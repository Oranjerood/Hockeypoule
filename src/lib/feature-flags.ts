// Simple feature flags for functionality that's temporarily hidden from the
// live site but should stay fully built so it can be switched back on later
// with a one-line change (no code needs to be re-written or restored).
export const CUSTOM_COMPETITIONS_ENABLED = false;
